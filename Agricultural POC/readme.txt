pip install streamlit streamlit-authenticator pyyaml transformers joblib numpy torch torchvision pillow opencv-python ultralytics streamlit-webrtc av scikit-learn

"To install all necessary modules adjust relevant file paths in app.py"
"To run app after installing all modules go to terminal and go to current directory where app.py is and then type 'streamlit run app.py' "
"In the config file usernames and passwords are stored and can be added"
"For default username :jsmith; password: abc " 
ideally run in a python environment

